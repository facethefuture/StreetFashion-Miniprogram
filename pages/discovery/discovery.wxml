<view class="container">
  <view> 
      <view wx:for="{{moduleList}}" wx:key="{{index}}" class="padding-xl radius shadow-blur bg-red margin-top bg-img module_name" style="background-image:url({{host + item.coverImage}});">
        {{item.name}}
      </view>
  </view> 
</view>
