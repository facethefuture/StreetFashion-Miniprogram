<view class="cu-bar search bg-white">
  <view class='search-form round'>
    <text class="icon-search"></text>
    <input bindconfirm='seachPicture' bindinput='edit' type="text" placeholder="搜索图片" confirm-type="search"></input>
  </view>
  <view class='action'>
    <button class='cu-btn bg-green shadow-blur round' bindtap="seachPicture">搜索</button>
  </view>
</view>

<view wx:for="{{list}}" wx:key="{{index}}" class="margin_bottom">
  <view class="cu-card case {{isCard?'no-card':''}}">
  <view wx:for="{{item}}" wx:for-item="items" wx:key="{{index}}" class="cu-item shadow">
    <view class='image'>
      <image src="{{host + items.coverImage}}" mode="widthFix"></image>
      <view class="cu-tag bg-blue">福利</view>
      <view class='cu-bar bg-shadeBottom'>街头小仙女</view>
    </view>
      <view class='padding-sm flex flex-wrap'>
      <view class="padding-xs" >
        <view>{{items.description}}</view>
      </view>
    </view>

    <view class='padding-sm flex flex-wrap'>
      <view class="padding-xs" >
        <view class='cu-tag bg-yellow'>#街拍</view>
      </view>
    </view>
  </view>
</view>
</view>

    
    <!-- <view class="swiper">
      <swiper
  indicator-dots="{{true}}"
  autoplay="{{true}}"
  interval="3000"
  duration="500"
  circular="{{true}}"
>
        <block wx:for="{{recommendList}}" wx:key="{{index}}" >
          <swiper-item>
            <view class="swiper_image">
             <image src="{{item}}" />
            </view>
           
          </swiper-item>
        </block>
      </swiper>
    </view> -->
